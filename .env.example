# Point at a DEDICATED `tower` database in your existing chores-postgres instance.
# Use a least-privilege role that owns only the tower DB (see README).
DATABASE_URL=postgres://tower:CHANGE_ME@chores-postgres:5432/tower

# Subpath this app is served under (matches your Caddy `handle /tower*` block).
BASE_PATH=/tower
PORT=8787
